<!DOCTYPE html>
<html>
<head>
    <!-- ✅ DYNAMIC TITLE -->
    <title>
      <%= typeof post !== "undefined" ? post.title + " | " : "" %>
      <%= setting.siteName || "Global Fresh News" %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/favicon.png">
    <link rel="stylesheet" href="/style.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

<nav class="navbar">

    <div class="navbar-top">

        <div class="nav-left">
            <a href="/">
                <img src="/uploads/<%= setting.logo || 'logo.png' %>" class="logo-img">
            </a>
        </div>

        <div class="nav-right">
            <a href="/admin" class="admin-link">Dashboard</a>
            <a href="/logout" class="admin-link">Logout</a>
        </div>

    </div>

</nav>
<div class="admin-container">

<h2>Pages</h2>

<a href="/admin/pages/new">+ Create Page</a>

<% pages.forEach(page => { %>
  <div class="admin-card">
    <h3><%= page.title %></h3>

    <a href="/admin/pages/edit/<%= page._id %>">Edit</a>

  <form action="/admin/pages/delete/<%= page._id %>" method="POST"
    onsubmit="return confirm('Are you sure you want to delete this post?')">
    <button type="submit">Delete</button>
  </form>
  </div>
<% }) %>

</div>

</body>
</html>